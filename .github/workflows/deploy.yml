name: Deploy

on:
  push:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        lfs: 'true'
    
    - name: Copy repository contents via scp
      uses: appleboy/scp-action@master
      env:
        HOST: ${{ secrets.HOST }}
        USERNAME: ${{ secrets.USERNAME }}
        PORT: ${{ secrets.PORT }}
        KEY: ${{ secrets.SSHKEY }}
      with:
        source: "."
        target: "/home/jhlfrfufyfn/bel-tts-server"
        
    - name: Building docker container by executing remote command
      uses: appleboy/ssh-action@master
      with:
        HOST: ${{ secrets.HOST }}
        USERNAME: ${{ secrets.USERNAME }}
        PORT: ${{ secrets.PORT }}
        KEY: ${{ secrets.SSHKEY }}
        script: sudo docker-compose -f /home/jhlfrfufyfn/bel-tts-server/docker-compose.yml -f /home/jhlfrfufyfn/bel-tts-server/docker-compose.prod.yml build  
        
      
    - name: Executing docker container by executing remote command
      uses: appleboy/ssh-action@master
      with:
        HOST: ${{ secrets.HOST }}
        USERNAME: ${{ secrets.USERNAME }}
        PORT: ${{ secrets.PORT }}
        KEY: ${{ secrets.SSHKEY }}
        script: sudo docker-compose -f /home/jhlfrfufyfn/bel-tts-server/docker-compose.yml -f /home/jhlfrfufyfn/bel-tts-server/docker-compose.prod.yml up
